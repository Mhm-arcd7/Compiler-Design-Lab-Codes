%{
#include <stdio.h>
int line_count = 0, word_count = 0, char_count = 0;
%}

%%
\n  	{ line_count++;}
[ \t]+
[^ \t\n]+ { word_count++; char_count += yyleng; }
%%

int main() {
	yylex();
	printf("Lines: %d\n", line_count);
	printf("Words: %d\n", word_count);
	printf("Characters: %d\n", char_count);
	return 0;
}

